/* asm_arm.inc -- ARM assembly stub for uECC
 *
 * This project builds uECC with portable C fallbacks for all platforms
 * (see platform-specific.inc).  All asm_* flags remain at their default
 * value of 0, so uECC.c will use its built-in C implementations for
 * every VLI operation.  No ARM-specific assembly is emitted.
 *
 * Copyright 2014, Kenneth MacKay. Licensed under the BSD 2-clause license.
 */

/* No assembly definitions — C fallbacks are used for all operations. */
